# bytebeat-player

## ToDo:
- merge Classic (C) and Javascript (JS) engines
- improve Editor window (relocate Zoom/Engine/Format to Settings window)
- update ExportToWav() to include user input for exported sample length instead 30s
- expand Sample Rate list to additionally accept custom sample rate (append input box to list)
- add expandable categories (lists) in Presets window